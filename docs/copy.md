# 对外话术

对外文案的唯一源头：README、官网、Releases 说明、设置「关于」页均从此档派生，禁止各场合另起炉灶。改文案先改这里，再同步各载体。

命名遵循 [product.md](product.md)：首次出现用「汐 TideBar」，行文用「汐」。

## 一、定位与受众

- 定位：macOS 零占用窗口任务栏——把 Dock 占的空间还给屏幕，把窗口管理做得比 Dock 更好。
- 受众：屏幕空间紧张的小屏 MacBook 用户；多窗口多任务、讨厌常驻界面噪音的人。
- 一句话差异：市面 Dock 类产品都在做「更好的 Dock」，汐做「消失的 Dock」——细线收起态没有先例。

## 二、Slogan

主推（首图大字用）：

> 平时是一条线，需要时是一片海。

备选：

- 把 Dock 收成一条线。
- Dock 退成一条线，窗口如潮可达。

英文（官网/仓库用，待定）：

- The Dock, faded to a line.
- A hairline at rest, a sea of windows at hand.

## 三、一段话 pitch

汐 TideBar 是一款 macOS 窗口任务栏。平时，它只是屏幕底部一条若隐若现的细线；鼠标靠近，便如潮汐般展开为应用栏——每个窗口的状态点点可见，点击即达。长按任意图标，「潮涌」展开该应用的全部窗口，连最小化的也能一键还原。开启接管，系统 Dock 隐去，屏幕底部只剩这条线，小屏 MacBook 也能多出一整行空间。只需辅助功能一项权限，不读屏幕内容，不联网。

## 四、核心功能

### 汐线 · 零占用

空闲时是贴着屏幕底边的一条 2~3pt 细线，随亮暗模式自适应，几乎不可见；鼠标靠近即潮汐般展开，离开即收。

### 点点 · 窗口状态可见

图标下的小点如实反映窗口：实心是活跃窗口，空心是最小化。回答的不是「哪些 app 在运行」，而是「每个窗口都在哪」。

### 潮涌 · 直达任意窗口

长按（或 ⌥+点击）图标展开窗口列表，按窗口标题直达，最小化的点击即还原。隐藏 Dock 后无处安放的最小化窗口，在这里有了落点。

### 涟漪 · 克制的通知

新角标到达时细线轻涌一次；未被查看就持续微微荡漾，展开即确认。不靠抢占注意力说「有事」，靠持续在场说「还没被知道」。

## 五、权限与隐私（标准话术）

- 只要一项权限：辅助功能——用于列出与还原窗口。
- 不请求屏幕录制：汐不显示窗口缩略图，不读屏幕内容。
- 零网络：不联网、无遥测、无账号。

> 发布前整体复核一次：新增功能若改变上述承诺，先改这里再对外。

## 六、命名故事

汐，傍晚的潮。安静时收敛成一条线，是汐；需要时窗口铺开而来，是潮涌。收与放，是潮汐的两种形态，也是这款软件的两种状态。

概念词与代码标识见 [product.md](product.md)（汐线 / 潮涌 Surge）。

## 七、FAQ

**汐和系统 Dock 什么关系？**
替代而非增强。开启「接管」后，汐隐藏系统 Dock 的可见入口并接替它；不开启也可以共存使用。

**最小化的窗口去哪了？**
图标下的空心点就是。长按或 ⌥+点击图标展开潮涌列表，点击即还原。

**为什么要辅助功能权限？**
列出与还原窗口依赖 macOS 辅助功能 API。这是汐请求的唯一权限。

**会显示窗口缩略图吗？**
不会。汐不读屏幕内容，因此永远不需要屏幕录制权限。

**全屏应用下会打扰吗？**
全屏行为可在设置中心调整。

**汐收费吗？**
开源软件，免费使用；许可证见仓库 LICENSE。

## 八、对比（官网/README 备用）

| | 系统 Dock | uBar | DockDoor | 汐 |
|---|---|---|---|---|
| 空闲占用 | 常驻一整条 | 常驻一整条 | 无（按需悬浮） | 一条细线 |
| 窗口粒度 | 仅 app + 运行点 | 每窗口一格（标题） | 悬停缩略预览 | app + 点点 / 潮涌列表 |
| 最小化窗口 | 堆在右侧难辨认 | 有显示 | 可见可点 | 空心点 + 潮涌直达 |
| 权限 | — | — | 屏幕录制 | 仅辅助功能 |

> 对外发布前复核竞品行描述与当期版本行为，避免失实。

## 九、载体派生规则

| 载体 | 取材 |
|---|---|
| README 首屏 | slogan + pitch + 演示 GIF + 四条功能 + 权限话术 + 下载（GitHub Releases）+ LICENSE |
| 官网落地页 | 同 README，另加对比表与 FAQ |
| Releases 说明 | 按版本列变化，沿用功能名（汐线/点点/潮涌/涟漪） |
| 设置「关于」页 | 图标 + 「汐 TideBar」+ slogan + 版本 + 版权 + 反馈渠道（GitHub Issues） |

## 十、待定项

- LICENSE 协议：开源已定，协议未选（MIT / Apache-2.0 / GPL-3.0 等），影响 README、仓库与官网页脚——需拍板。
- 演示素材：汐线→展开→潮涌 GIF + 接管前后对比截图，见 [branding 立案](../plans/todo-2026-09-06-branding.md)。
- 英文版文案：本地化整体延后（见 plans/），官网需要英文时再补。
- 官网落地页：发布前完成，随 branding 立案产出。

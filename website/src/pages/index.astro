---
import SiteLayout from "../layouts/SiteLayout.astro";
import PageContent from "../components/PageContent.astro";
---
<SiteLayout
  title="TideBar — A zero-footprint window taskbar for macOS"
  description="TideBar is a zero-footprint window taskbar for macOS: a hairline at rest, every window at hand. One permission, no screen recording, no telemetry."
>
  <PageContent />
</SiteLayout>

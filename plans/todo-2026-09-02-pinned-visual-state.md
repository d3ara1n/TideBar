# 固定状态视觉表达

## 背景

应用的固定状态与运行状态彼此独立。右键菜单可以承担固定/取消固定操作，但图标栏仍需让用户直接判断条目是固定项，还是仅因正在运行而临时出现。

## 目标

- 为固定状态建立无需打开菜单即可识别的视觉表达。
- 不采用固定区与临时运行区分区。
- 保持窗口点只表达窗口数量与最小化状态，不混入固定语义。
- 方案符合 TideBar 的低存在感原则，避免常驻高噪声徽标。

## 待定

视觉语言及其布局需单独讨论确认后实施。
